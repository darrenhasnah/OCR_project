Persiapan (Windows PowerShell):
1) Buat virtualenv dan aktifkan
```powershell
python -m venv .venv; .\.venv\Scripts\Activate.ps1
```
2) Install dependency
```powershell
pip install -r requirements.txt
```

Cara pakai (console)
.\.venv\Scripts\activate


run

python "d:\uph\Semester 7\Grafika Komputer\UAS\UAS_Grafika\ocr_finetune\inference_paddleocr_postprocess.py"